actor: AcceptanceTester
modules:
  enabled:
    - WPWebDriver
    - Asserts
    - \Helper\Acceptance
  config:
    WPWebDriver:
      # REPLACE WITH YOUR URL:
      url: 'http://ec2-54-211-251-233.compute-1.amazonaws.com'

      adminUsername: 'admin'
      adminPassword: 'password'
      adminPath: '/wp-admin'
      port: 4444
      browser: phantomjs